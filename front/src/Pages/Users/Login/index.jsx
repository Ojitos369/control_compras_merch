import { useStates } from '../../../Hooks/useStates';

const Login = props => {
    const { s, f } = useStates();
    return (
        <div>
            <h1>Login</h1>
        </div>
    )
}
export { Login };